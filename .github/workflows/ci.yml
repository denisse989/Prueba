name: Script de prueba
on: [push]

jobs:
  primera:
   uses: actions/checkout@v1
   name: Primera Tarea
   runs-on: ubuntu-latest
   steps:
    - name: Primer paso
      run: echo "Hola Clase"
    - name: Listado de archivos
      run: ls